# Static BakaBoost Version

Open `index.html` to preview the static HTML/CSS/JS version.

For reliable Discord webhook posting, run:

```bash
node server.js
```

Then open:

```text
http://127.0.0.1:3005
```

The main files are:

- `index.html`
- `styles.css`
- `app.js`
- `server.js`
